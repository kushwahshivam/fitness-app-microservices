server:
  port: 8081


spring:
  datasource:
#    url: jdbc:postgresql://localhost:5432/fitness_user_db
#    url : jdbc:postgresql://fitness-user-db.col6ywkuoevc.us-east-1.rds.amazonaws.com/fitness_user_db
    url: ${JDBC_DATABASE_URL}
    username: postgres
    password: root



  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/